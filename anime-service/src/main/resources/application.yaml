spring:
  application:
    name: anime-service
  threads:
    virtual:
      enabled: true

server:
  servlet:
    context-path: /
  error:
    include-stacktrace: never
  tomcat:
    threads:
      max: 20

logging:
  level:
    academy.devdojo: debug

database:
  url: ${LOCAL_URL:localhost:3306/anime}:${DB_PORT:3306}
  username: ${LOCAL_USERNAME:kelsen}
  password: ${LOCAL_PASSWORD:brito}